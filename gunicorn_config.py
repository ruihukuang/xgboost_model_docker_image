bind = "0.0.0.0:8000"

# Number of worker processes
workers = 4  # Adjust based on your application's needs and server capacity

# Timeout settings
timeout = 900  # Worker timeout in seconds
keepalive = 900  # Keep-alive timeout in seconds
